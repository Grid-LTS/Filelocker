#include <filesystem>
#include <iostream>
#include "../headers/main.h"
using namespace std;

void listen_notification_events(const filesystem::path &watch_path)
{
    cout << "Mac OSX not supported." << endl;
}